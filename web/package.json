{
  "name": "kanban-web",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.7"
  },
  "comment-overrides": "This dev machine blocks rollup's native .node binary (Windows Application Control), so we build with the rollup team's WASM build everywhere. Same output, one code path, a little slower.",
  "overrides": {
    "rollup": "npm:@rollup/wasm-node@^4.63.1"
  }
}
