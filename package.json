{
  "name": "pandan-board",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm --prefix web ci --no-audit --no-fund && npm --prefix web run build",
    "start": "node start.js",
    "dev": "node --watch server/index.js",
    "check": "node --check setup.js && node --check start.js && node --check connect.js && node --check server/index.js && node --check server/paths.js && node --check server/db.js && node --check server/auth.js && node --check server/routes.js && node --check server/mcp.js && node --check server/events.js",
    "smoke": "npm --prefix web run smoke",
    "setup": "node setup.js",
    "serve": "node start.js",
    "connect": "node connect.js",
    "postinstall": "node setup.js --quiet",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "express": "^5.1.0",
    "zod": "^4.5.4"
  },
  "description": "A small self-hosted kanban board with an MCP server, so you can see what your agents are doing.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/IdoGigi/pandan.git"
  },
  "bugs": {
    "url": "https://github.com/IdoGigi/pandan/issues"
  },
  "homepage": "https://github.com/IdoGigi/pandan",
  "keywords": [
    "kanban",
    "mcp",
    "agents",
    "claude",
    "self-hosted",
    "sqlite"
  ],
  "bin": {
    "pandan": "start.js"
  },
  "files": [
    "server/",
    "web/dist/",
    "examples/",
    "start.js",
    "setup.js",
    "connect.js",
    "README.md",
    "API.md",
    "LICENSE"
  ]
}
